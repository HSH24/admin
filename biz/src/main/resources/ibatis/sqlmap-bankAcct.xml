<?xml version="1.0" encoding="GB2312"?>
<!DOCTYPE sqlMap
	PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="bankAcct">
	
	<typeAlias alias="bankAcct" type="com.hsh24.admin.api.bankAcct.bo.BankAcct" />
	
	<select id="getBankAcctStats" parameterClass="bankAcct" resultClass="bankAcct">
	<![CDATA[
		SELECT SUM(t.cur_bal) AS curBal
		  FROM dms_tb_bank_account t,
			   dms_tb_account p,
			   dms_tb_org_shop q
		 WHERE t.acc_id = p.acc_id
		   AND t.shop_id = p.shop_id
		   AND t.shop_id = q.shop_id
		   AND t.state = 'U'
		   AND p.state = 'U'
		   AND q.state = 'U'
		   AND p.acc_code = #accCode#
		   AND q.org_id = #orgId#
	]]>
	</select>
	
</sqlMap>