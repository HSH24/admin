<?xml version="1.0" encoding="GB2312"?>
<!DOCTYPE sqlMap
	PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="shop">
	
	<typeAlias alias="shop" type="com.hsh24.admin.api.shop.bo.Shop" />
	
	<select id="getShop" parameterClass="shop" resultClass="shop">
	<![CDATA[
		SELECT t.shop_id AS shopId,
			   t.shop_name AS shopName,
			   t.address,
			   t.user_id AS userId
		  FROM mdm.mdm_tb_shop t
		 WHERE t.state = 'U'
		   AND t.shop_id = #shopId#
	]]>
	</select>
	
	<select id="getOrgShopCount" parameterClass="shop" resultClass="java.lang.Integer">
	<![CDATA[
		SELECT COUNT(1)
		  FROM dms_tb_org_shop t
		 WHERE t.STATE = 'U'
		   AND t.org_id = #orgId#
	]]>
	</select>
	
	<select id="getOrgShopList" parameterClass="shop" resultClass="shop">
	<![CDATA[
		SELECT t.shop_id AS shopId
		  FROM dms_tb_org_shop t
		 WHERE t.state = 'U'
		   AND t.org_id = #orgId#
	]]>
	</select>
	
</sqlMap>